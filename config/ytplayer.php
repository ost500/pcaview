<?php

return [
    /*
    |--------------------------------------------------------------------------
    | YTPlayer API Signature Secret
    |--------------------------------------------------------------------------
    |
    | HMAC 서명 검증을 위한 비밀키입니다.
    | 모바일 앱과 서버가 동일한 비밀키를 공유해야 합니다.
    |
    */
    'signature_secret' => env('YTPLAYER_SIGNATURE_SECRET', ''),

    /*
    |--------------------------------------------------------------------------
    | Disable Signature Verification
    |--------------------------------------------------------------------------
    |
    | 개발/테스트 환경에서 서명 검증을 비활성화합니다.
    | ⚠️ 프로덕션에서는 절대 true로 설정하지 마세요!
    |
    */
    'disable_signature_verification' => env('YTPLAYER_DISABLE_SIGNATURE', false),

    /*
    |--------------------------------------------------------------------------
    | Request Timeout
    |--------------------------------------------------------------------------
    |
    | 요청의 타임스탬프가 현재 시간과 이 값보다 차이나면 거부합니다. (초 단위)
    |
    */
    'request_timeout' => env('YTPLAYER_REQUEST_TIMEOUT', 300), // 5분

    /*
    |--------------------------------------------------------------------------
    | Duplicate Prevention
    |--------------------------------------------------------------------------
    |
    | 동일한 사용자의 중복 적립을 방지하기 위한 설정입니다.
    |
    */
    'duplicate_prevention' => [
        'enabled' => env('YTPLAYER_DUPLICATE_PREVENTION', true),
        'window'  => env('YTPLAYER_DUPLICATE_WINDOW', 60), // 60초 이내 중복 방지
    ],
];
